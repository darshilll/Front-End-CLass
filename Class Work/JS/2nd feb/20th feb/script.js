const firstName = prompt('Enter your name: ');
const digits = prompt(`${firstName}, enter the count of digits you wish to have in the passwords: `);
const socialMedia = prompt(`Enter 3 media comma seperated`);
// console.log(socialMedia); 
let wishToPutPassword = prompt(`Enter the media name you wish to retrieve the password of: `);
let medias = ['instagram' , 'snapchat' , 'facebook'];
for(let i = 0; i < medias.length;i++){
    if(medias){
        alert = (`${firstName}, the password is`)
    }
    else{
        alert = (`invalid password`)
    
    }
    console.log(medias);
}

function media(){
    
}